# Быстрый старт

## 🚀 Запуск за 5 минут

### Вариант 1: Docker (Самый простой способ)

```bash
# 1. Запустить все сервисы
docker-compose up -d

# 2. Проверить что все работает
curl http://localhost:8080/vacancy

# 3. Открыть phpMyAdmin (опционально)
# http://localhost:8081
# Логин: root
# Пароль: rootpassword
```

**Готово!** API работает на `http://localhost:8080`

### Вариант 2: Локальный запуск

```bash
# 1. Создать базу данных
mysql -u root -p
CREATE DATABASE vakansii_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 2. Настроить .env файл
cp .env.example .env
# Отредактировать .env с вашими настройками БД

# 3. Запустить приложение
go run main.go
```

API будет доступен на `http://localhost:8080`

## 📝 Первые шаги

### 1. Создать тестовую вакансию

```bash
curl -X POST http://localhost:8080/vacancy \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Go Developer",
    "description": "Требуется опытный Go разработчик",
    "salary": 180000
  }'
```

### 2. Получить список вакансий

```bash
curl http://localhost:8080/vacancy
```

### 3. Поиск вакансий

```bash
curl "http://localhost:8080/vacancy/search?q=Go"
```

## 🛠️ Использование Makefile

```bash
# Собрать проект
make build

# Запустить локально
make run

# Запустить Docker контейнеры
make docker-up

# Остановить Docker контейнеры
make docker-down

# Показать логи
make docker-logs

# Форматировать код
make fmt

# Обновить зависимости
make tidy
```

## 📖 Дополнительная документация

- [README.md](README.md) - Полная документация
- [API_EXAMPLES.md](API_EXAMPLES.md) - Примеры использования API
- [PROJECT_INFO.md](PROJECT_INFO.md) - Детали реализации

## ❓ Частые вопросы

### Порт 8080 уже занят

Измените `SERVER_PORT` в `.env` файле:

```env
SERVER_PORT=8000
```

### Ошибка подключения к БД

Проверьте настройки в `.env`:

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=vakansii_db
```

### MySQL не запускается в Docker

```bash
# Остановить все контейнеры
docker-compose down -v

# Запустить заново
docker-compose up -d
```

## 🔍 Проверка работы

```bash
# Проверить что API работает
curl http://localhost:8080/vacancy

# Должен вернуть:
# {"data":[],"pagination":{"page":1,"pageCount":0,"pageSize":10,"total":0}}
```

## 🎉 Готово!

Ваш API сервис вакансий готов к работе!

Используйте [API_EXAMPLES.md](API_EXAMPLES.md) для примеров всех возможных запросов.
