# API Сервиса Вакансий на Go + Gin

Backend-сервис для хранения и управления вакансиями, реализованный на Go с фреймворком Gin и REST API интерфейсом.

## Основные возможности

- ✅ **Полнотекстовый поиск** с FULLTEXT индексами MySQL
- ✅ **Многослойная архитектура** (Controllers → Services → Repositories)
- ✅ Получение списка вакансий с пагинацией (10 записей на страницу)
- ✅ Сортировка по зарплате, дате создания и релевантности
- ✅ Получение конкретной вакансии с выборочным отображением полей
- ✅ CRUD операции (создание, обновление, удаление)
- ✅ **Rate Limiting** (100 запросов в час на IP)
- ✅ CORS для интеграции с frontend
- ✅ Поддержка дополнительных полей в формате JSON
- ✅ **Docker** поддержка с docker-compose

## Технологии

- **Language:** Go 1.23
- **Framework:** Gin
- **ORM:** GORM
- **Database:** MySQL 8.0
- **API:** REST JSON

## Быстрый старт

### 🐳 Вариант 1: Docker (рекомендуется)

```bash
# Запустить все сервисы
docker-compose up -d

# API будет доступен по адресу: http://localhost:8080
# phpMyAdmin: http://localhost:8081
```

### 💻 Вариант 2: Локальная установка

#### Требования

- Go 1.23 или выше
- MySQL 8.0 или выше
- Git

#### Установка

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd vakansii-back-go

# 2. Установить зависимости
go mod download

# 3. Настроить .env файл
cp .env.example .env
# Отредактируйте .env файл с вашими настройками БД

# 4. Создать базу данных
mysql -u root -p
CREATE DATABASE vakansii_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 5. Запустить приложение (миграции выполнятся автоматически)
go run main.go
```

API будет доступен по адресу: `http://localhost:8080`

## Структура проекта

```
vakansii-back-go/
├── config/             # Конфигурация приложения
├── controllers/        # HTTP обработчики (тонкий слой)
├── services/           # Бизнес-логика
├── repositories/       # Работа с базой данных
├── models/             # Модели данных (GORM)
├── migrations/         # Миграции базы данных
├── middleware/         # Middleware (CORS, Rate Limiting)
├── main.go             # Точка входа
├── .env                # Конфигурация окружения
├── Dockerfile          # Docker образ
└── docker-compose.yml  # Docker Compose конфигурация
```

## API Документация

### Получение списка вакансий

```bash
# Получить первую страницу
curl "http://localhost:8080/vacancy"

# С сортировкой по зарплате
curl "http://localhost:8080/vacancy?sort=salary&order=desc"

# Вторая страница
curl "http://localhost:8080/vacancy?page=2"
```

**Ответ:**
```json
{
  "data": [
    {
      "id": 1,
      "title": "PHP Developer",
      "description": "Требуется опытный разработчик",
      "salary": 150000,
      "additional_fields": {"location": "Москва"},
      "created_at": "2025-01-10T12:00:00Z",
      "updated_at": "2025-01-10T12:00:00Z"
    }
  ],
  "pagination": {
    "total": 25,
    "page": 1,
    "pageSize": 10,
    "pageCount": 3
  }
}
```

### Полнотекстовый поиск

```bash
# Поиск по ключевым словам
curl "http://localhost:8080/vacancy/search?q=PHP%20разработчик"

# С пагинацией и сортировкой
curl "http://localhost:8080/vacancy/search?q=developer&page=2&sort=relevance"
```

### Получение конкретной вакансии

```bash
# Все поля
curl "http://localhost:8080/vacancy/1"

# Только определенные поля
curl "http://localhost:8080/vacancy/1?fields=title,salary"
```

### Создание вакансии

```bash
curl -X POST http://localhost:8080/vacancy \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Go Developer",
    "description": "Требуется опытный Go разработчик",
    "salary": 180000,
    "additional_fields": {
      "company": "Tech Corp",
      "location": "Москва",
      "remote": true
    }
  }'
```

**Ответ:**
```json
{
  "success": true,
  "id": 13,
  "message": "Вакансия успешно создана"
}
```

### Обновление вакансии

```bash
curl -X PUT http://localhost:8080/vacancy/1 \
  -H "Content-Type: application/json" \
  -d '{
    "salary": 200000,
    "title": "Senior Go Developer"
  }'
```

### Удаление вакансии

```bash
curl -X DELETE http://localhost:8080/vacancy/1
```

## Конфигурация

Настройки приложения хранятся в файле `.env`:

```env
# Сервер
SERVER_PORT=8080
GIN_MODE=debug

# База данных
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=vakansii_db
DB_CHARSET=utf8mb4

# CORS
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600
```

## Docker

### Запуск с Docker Compose

```bash
# Запустить все сервисы
docker-compose up -d

# Просмотреть логи
docker-compose logs -f app

# Остановить все сервисы
docker-compose down

# Остановить и удалить данные
docker-compose down -v
```

### Сервисы

- **app** - Go приложение (порт 8080)
- **mysql** - MySQL 8.0 (порт 3306)
- **phpmyadmin** - phpMyAdmin (порт 8081)

## Разработка

### Сборка проекта

```bash
go build -o main .
```

### Запуск в режиме разработки

```bash
go run main.go
```

### Форматирование кода

```bash
go fmt ./...
```

### Проверка зависимостей

```bash
go mod tidy
```

## Архитектура

Проект следует многослойной архитектуре:

1. **Controllers** - Обрабатывают HTTP запросы, валидируют входные данные
2. **Services** - Содержат бизнес-логику
3. **Repositories** - Работают с базой данных через GORM
4. **Models** - Определяют структуры данных

## Особенности реализации

### Полнотекстовый поиск

Использует FULLTEXT индексы MySQL для эффективного поиска по полям `title` и `description`:

```go
db.Where("MATCH(title, description) AGAINST (? IN NATURAL LANGUAGE MODE)", query)
```

### Rate Limiting

Ограничивает количество запросов с одного IP адреса (100 запросов в час по умолчанию).

### CORS

Настроен для работы с frontend приложениями. Разрешенные origins задаются через `CORS_ORIGIN` в `.env`.

### Пагинация

Все списковые endpoints поддерживают пагинацию (10 записей на страницу).

## Примеры запросов

См. файл `API_EXAMPLES.md` для дополнительных примеров использования API.

## Лицензия

MIT
